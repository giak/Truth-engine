{
  "version": 1,
  "task": "truth-engine",
  "protected_branches": ["main", "master"],
  "tests": ["python3 -m pytest tests/extractors/ -q"],
  "checks": [
    {
      "name": "no-em-dash-in-articles",
      "cmd": "grep -rl '—' articles/ 2>/dev/null && exit 1 || exit 0",
      "fail_message": "tiret cadratin (U+2014) trouvé dans articles/ — interdit par le contrat (scope Phase 3)"
    }
  ],
  "naming": {
    "enabled": true,
    "note": "kebab-case uniquement (décision 2026-08-17). Scope investigations/ d'abord : articles/ et outputs/ contiennent du legacy non conforme.",
    "dirs": ["investigations/"],
    "pattern": "^\\d{4}-\\d{2}-\\d{2}(_\\d{2}-\\d{2})?_[a-z0-9]+(-[a-z0-9]+)*_(ARTICLE|HYPER_MATRICE|ARCHITECTURE|SATURATION_AUDIT|REGISTRE|INVESTIGATION)\\.md$"
  }
}
