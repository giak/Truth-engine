# INV-158 — RECOVERY / COVERAGE NOTE

Registry terminal status: `CLOSED` / `DELIVERY_PASS_R3P1`.

Available physical evidence in this bundle:
- original RUN_CARD;
- reclassification that selected the run;
- exact RUN_STATE captured while still `OPEN`, after checkpoint 17 and before final serialization.

Unavailable on currently recoverable file surfaces:
- terminal `_INVESTIGATION.md`;
- terminal `_CERTIFICATION.json`;
- terminal `RUN_HANDOFF.md`;
- terminal `_MNEMO_SNAPSHOT.json`.

The current registry states `CLOSED / DELIVERY_PASS_R3P1`, but this bundle **does not reconstruct or fabricate the missing terminal bytes**. Treat INV-158 as `TERMINAL_STATUS_ATTESTED / PHYSICAL_TERMINAL_ARTIFACTS_INCOMPLETE`.
