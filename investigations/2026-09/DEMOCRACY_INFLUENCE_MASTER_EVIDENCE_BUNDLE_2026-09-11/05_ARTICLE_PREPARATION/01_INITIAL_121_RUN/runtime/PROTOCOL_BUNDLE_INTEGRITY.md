# Protocol bundle integrity

`bin/verify.sh` executed on the local Article Protocol bundle: `VERIFY = PASS_BUNDLE_INTEGRITY`.

This is an integrity/contract-marker check only and is **not** treated as an independent semantic/product audit.
